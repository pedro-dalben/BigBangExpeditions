# Goal 00 — Test Matrix

**Pack:** DeceasedCraft - Urban Zombie Apocalypse (Forge 1.20.1, LostCities 7.4.11)  
**Instance:** `/home/pedro/Documents/curseforge/minecraft/Instances/DeceasedCraft - Urban Zombie Apocalypse`  
**Status:** Static analysis complete; live server runs pending → most entries are `BLOCKED` (evidence incomplete) until staging server validates.  
**Procedure ref:** `docs/investigations/goal-00-persistent-exploration.md` §3–5

> Columns: `Expected` = desired safe behavior; `Actual` = observed on live run (or predicted from static config); `Evidence` = file/log/hash path.

| Test | Preconditions | Operation | Expected | Actual | PASS/FAIL/BLOCKED | Evidence |
|------|---------------|-----------|----------|--------|-------------------|----------|
| **T01 — Version inventory** | Client instance available | `ls mods | grep -iE "lostcities|lootr|opac"` + `cat manifest.json` | All versions enumerated | Found: LostCities 7.4.11, Lootr 0.7.35.94 (disable=true), OPAC 0.25.8, Hordes 1.5.4c, Create 6.0.6, IE 10.2.0, RS 1.12.4. 306 jars. | **PASS** | `mods/lostcities-1.20-7.4.11.jar`, `mods/lootr-forge-1.20-0.7.35.94.jar`, `mods/open-parties-and-claims-forge-1.20.1-0.25.8.jar`, `config/lootr-common.toml:62`, `config/lostcities/common.toml` |
| **T02 — Baseline capture** | Staging server with fixed seed + `deceasedcraft_onlycities` profile, sector `r.0.0–r.3.3` (0..2047 blocks) unmodified | Stop, `sha256sum world/region/r.*.mca world/entities/r.*.mca world/poi/r.*.mca world/data/*.dat`, dump `level.dat seed`, probe JSON | Hashes recorded, containers counted | Not yet executed — no `world/` on client | **BLOCKED** | TODO: `scripts/baseline.sh` → `baseline_region.sha256`, `probe.json` |
| **T03 — Delete only chunk terrain (E1)** | Baseline exists, server stopped | `rm world/region/r.0.0.mca` only, restart | **REFUSED** or ghost detection — POI/entities/SavedData must not survive | Predicted: POI+entities+Lootr ChestData+OPAC remain → ghost chests/claims. Static: `Lootr/DataStorage` retains `isAwarded` | **BLOCKED** (predicted FAIL if attempted) | `world/data/lootr*.dat` still has `tileId` entries; `world/poi/r.0.0.mca` still present |
| **T04 — Delete chunk+entities (E2)** | Baseline, stopped | `rm region + entities/r.0.0.mca`, restart | POI+SavedData remain → must still REFUSE | Predicted: OPAC claims survive → building allowed in regenerated sector would overwrite claim | **BLOCKED** | `world/poi/r.0.0.mca` + `ChestData` |
| **T05 — Delete chunk+entities+POI (E3)** | Baseline, stopped | `rm region+entities+poi/r.0.0.mca` | SavedData remains → must REFUSE | Predicted: Lootr stale `ChestData`, RS `refinedstorage.dat` ghost | **BLOCKED** | `DataStorage.Loaded tileId from already migrated file` string |
| **T06 — Full offline wipe + scrub (E5)** | Baseline, stopped, OPAC no intersect, no BE/entities in sector | `rm region+entities+poi` + NBT scrub `ChestData` where `pos in bounds && dimension==overworld`, restart | City layout, roads, spawners, vanilla loot reappear bit-identical; no ghost claims | Not yet measured — procedure in `goal-00-persistent-exploration.md §5` | **BLOCKED** | TODO: `sha256(region)` post-regen, `nbt dump` spawner counts, `DataStorage` size before/after |
| **T07 — Online unload/regen (E6)** | Sector loaded, no players in sector | Call `ServerLevel.getChunkSource().chunkMap` unload API while server running | **REFUSED** — `chunk cannot be safely unloaded` or `RESET REFUSED` | Predicted: `ChunkHolder` tickets + `TileTicker` race → corruption, `DataStorage` memory stale | **BLOCKED** (expected FAIL) | `logs/latest.log` NPE / `cannot fetch data storage` |
| **T08 — Structure layout determinism** | Seed + profile pinned, sector `r.0.0` | Delete + regen (offline), compare building registry dump | Same buildings, roads, `parkChance`, `highway` | Static: `cityChance=1.0`, `LostCityChunkGenerator` deterministic per seed | **BLOCKED** (awaiting hash) | `lostcities debug` dump diff |
| **T09 — Spawner/BE return** | Same as T08 | `nbt count` spawners + `block_entities` before/after | Identical counts | `generateSpawners:true`, `generateLoot:true` in profile | **BLOCKED** | `nbtdump world/region/r.0.0.mca | grep -c "minecraft:mob_spawner"` |
| **T10 — Lootr per-player state (if enabled)** | Lootr enabled (currently disabled), Player A/B open same container at pos P | `AdvancementData.isAwarded` before/after regen | After regen without scrub: A/B still scored; after scrub: A/B can re-loot (dupe) | Static: `ChestData.isAwarded`, `getInstanceUuid(level,pos)` deterministic | **BLOCKED** (Lootr disable=true makes test N/A; vanilla loot duplicates) | `config/lootr-common.toml: disable=true` |
| **T11 — Vanilla loot duplication (Lootr disabled)** | Lootr disabled (current pack), chest with `LootTable` → Player A loots → regen | Regen re-rolls `LootTableSeed` → Player A gets new loot → duplication | **Expected duplication** → proves `PROGRESSION_ITEM` cannot be renewable without filter | Predicted: `research_paper_*` duplicates infinitely | **BLOCKED** (needs loot table audit) | `RandomizableContainerBlockEntity.LootTable` NBT |
| **T12 — Horde Buildings persistence** | Sector contains Horde Building (LostCities multi-building) | Regen offline | Building reappears (deterministic) ; Hordes entity timers unaffected | Static: Hordes has no `SavedData` worldgen, only `horde_data/tables/*.json` pools | **PASS** (static) | `The-Hordes-1.20.1-1.5.4c.jar` has no `worldgen` pkg; `hordes-common.toml` event timers |
| **T13 — Progression duplication classification** | KubeJS `research_paper_1..5` + `research_book` + `formula_x` | Audit loot tables via `kubejs/data/**/loot_tables` | `research_paper_*` → `PROGRESSION_ITEM` | Found: `materials.js: research_paper_1..5` + `cures.js: research_book` chain; no loot table grep yet | **PASS** (partial) | `kubejs/startup_scripts/items/materials.js:82-110`, `kubejs/server_scripts/recipes/deceasedcraft/cures.js` |
| **T14 — OPAC intersect inside** | Claim at chunk (10,10), sector (8..15,8..15) | `ServerClaimsManager.getDimension(OVERWORLD).getClaim(10,10)` | Returns non-null → `INTERSECTS` → REFUSE | Code probe: `ServerClaimsManager.class` + `ServerRegionClaims` 512×512 | **PASS** (static) | `xaero/pac/common/server/claims/ServerClaimsManager.class` strings |
| **T15 — OPAC partial boundary** | Claim at (15,15), sector (0..15,0..15) | Same iteration inclusive bounds | Intersects | Inclusive rect `effectiveLeft/Right` | **PASS** (static) | `ClaimsManager.tryClaimActionOverArea` rect logic |
| **T16 — OPAC party/personal + unloaded** | Party claim via `ftb-teams`, chunk not loaded | `DimensionDataStorage` lookup (not chunk) | Still intersects | OPAC stores in `world/data`, not chunk NBT | **PASS** (static) | `ServerClaimsManager` uses `Long2ObjectMap` per dimension |
| **T17 — OPAC fail-closed (world unavailable)** | `ServerClaimsManager.get==null` or `dim==null` during startup | Call intersection API | **REFUSE** | `DataStorage cannot fetch data storage` log pattern | **PASS** (spec) | API returns `FAIL_CLOSED` |
| **T18 — Create/IE/RS/SecurityCraft detection** | Sector contains Create shaft + IE capacitor + RS controller + SecurityCraft mine | `chunk.getBlockEntities()` iteration + `NetworkManager` SavedData scan | Any BE found → REFUSE | Static BE registry: `create:shaft`, `immersiveengineering:multiblock`, `refinedstorage:controller` | **PASS** (spec) | `world/region` NBT + `world/data/refinedstorage.dat` |
| **T19 — Prohibit building (dimension mode)** | Expedition dimension `bigbangexpeditions:expedition` with `claimsEnabled=false` | Try `/opac claim` in dimension | Rejected at OPAC perm level → no detection needed | Requires `dimensionsWithProfiles` + OPAC dimensionBlacklist | **BLOCKED** (dimension not yet created) | `config/lostcities/common.toml: dimensionsWithProfiles` |
| **T20 — Boundary seam** | Sector `r.0.0` (0..511), neighbor `r.1.0` (512..1023), highway crossing x=511→512 | Delete only `r.0.0`, restart | Seam cliff / half-road → document as known risk; prefer region-aligned large sector | Predicted from `Highways`, `Bridges` gen spanning cities | **BLOCKED** | TODO: screenshot `F3+G` + `nbtdump` road blocks |
| **T21 — Region alignment** | Sector bounds at chunk 31 vs 32 | Validate registry `bounds % 32 == 0` | Non-aligned → REFUSE | Region file = 32×32 chunks; OPAC region = 512 blocks | **PASS** (spec) | `ServerRegionClaims` 512×512, `RegionFileStorage` |
| **T22 — Online vs offline safety** | Same sector, compare both paths | Execute E6 vs E5 | Offline PASS, Online REFUSED | Table §10 in investigation doc | **PASS** (spec) | `DimensionDataStorage.save()` only on stop |
| **T23 — Determinism cycles** | Baseline → loot → regen → validate (×3) | `sha256(region)` after each regen | Hashes identical across 3 cycles | Not yet run | **BLOCKED** | TODO: `baseline→loot→regen→validate` hashes |
| **T24 — Fail-closed: player inside** | Player entity AABB inside sector | `level.getEntities(Player, AABB)` | REFUSE | `Player` in `world/entities` + online player list | **PASS** (spec) | `ServerLevel.getEntities` |
| **T25 — Fail-closed: invalid bounds** | `minX > maxX` or `bounds` not region-aligned or `width==0` | Validate before delete | REFUSE | Rect validation `maxRequestLength` | **PASS** (spec) | `ClaimsManager.tryClaimActionOverArea` bounds |
| **T26 — Fail-closed: dimension unavailable** | `server.getLevel(ResourceKey)` returns null | Get `ServerLevel` for sector dimension | REFUSE | `ServerClaimsManager` null path | **PASS** (spec) | `DimensionDataStorage` fetch fail log |
| **T27 — Fail-closed: chunk cannot unload** | Forceload ticket active on sector | Check `ForceLoadTicketManager` | REFUSE | `ClaimTicket` in `xaero/pac/common/server/claims/forceload` | **PASS** (spec) | `ServerClaimsManager` forceload |
| **T28 — Fail-closed: persistence cannot be evaluated** | `ChestData` NBT corrupted or `DataStorage==null` | NBT scrub step throws | REFUSE | `Lootr cannot determine...` strings | **PASS** (spec) | `DataStorage.class` error logs |
| **T29 — Fail-closed: validation fails** | Post-regen spawner count mismatch or `LootTable` missing | Compare pre/post probe JSON | REFUSE + keep backup | Validation step in §6 | **BLOCKED** | TODO: `probe.json` diff |
| **T30 — Fail-closed: unexpected mod data** | `world/data/refinedstorage.dat` network spanning sector, or unknown `world/data/*.dat` with pos-bound entries | Scan `world/data/*.dat` for pos in bounds | REFUSE until manual audit | RS `NetworkManager` graph not yet enumerated | **BLOCKED** | `unzip -l refinedstorage-1.12.4.jar` SavedData |

### Legend

- **PASS:** Static evidence or spec complete.
- **BLOCKED:** Requires live staging server run not yet performed on this host; do not assume success.
- **FAIL:** Would be destructive if executed live — must be refused.

### Next steps to unblock

Execute T02, T06, T08, T09, T11, T20, T23, T29, T30 on staging server with fixed seed; publish `baseline_region.sha256`, `probe.json`, and `logs/latest.log` extracts. Until then, Goal 00 remains `BLOCKED`.

